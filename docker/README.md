# Docker Configuration

Configuración para Docker y despliegue.

## Archivos

- `Dockerfile` - Configuración de la imagen de producción
- `docker-compose.yml` - Orquestación local (API, DB, Redis, etc.)
- `dokploy-config.yaml` - Configuración específica de Dokploy
